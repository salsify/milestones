import Component from '@ember/component';

/**
 * Shows or hides its content based on its given `@visible` arg.
 * The given block is always rendered, regardless of whether it's
 * visible to the user or not.
 */
export default class SlideHideComponent extends Component {
  public tagName = '';
}
